# For running/testing FDCS on your PC (Windows/Linux/Mac) before building the APK
kivy==2.3.0
pillow
numpy
tensorflow-cpu==2.15.0
opencv-python   # Kivy's Camera widget needs this as its webcam provider on Windows
plyer           # native "upload image" file picker (Home screen's Upload button)
pywin32         # plyer's Windows file-picker backend
